# TOGOSHOL — большой аудит сайта и список исправлений

Дата аудита: 04.06.2026  
Состояние: сайт уже показывает импортированные товары из VK, но визуально и продуктово ещё сырой.

## Выполнено 05.06.2026

- Отключён `scroll-snap`, который мешал обычной прокрутке и чтению.
- Убрано принудительное `min-height: 100svh` у всех секций.
- Добавлена отдельная секция полноценного каталога `ProductCatalog`.
- Все 26 VK-товаров теперь доступны не только в hero-ленте, но и в сетке каталога.
- Добавлены фильтры каталога по бюджету: `Все`, `до 60k`, `60–90k`, `90–150k`, `150k+`.
- Добавлена нормализация товарных данных в `src/lib/products.ts`.
- Из VK-описаний извлекаются цена числом, CPU, GPU, RAM, SSD, PSU и чистые specs.
- Hero-лента сокращена до 6 рекомендуемых сборок, чтобы первый экран не превращался в перегруженный каталог.
- Навигация “Сборки” теперь ведёт в полноценный каталог `#catalog`.
- Hero CTA “Смотреть сборки” ведёт в `#catalog`.
- CTA “Написать” переведён на `https://vk.me/tog_pc`.
- Конфигуратор доработан: добавлены выбор типа комплектующих, RAM и SSD.
- Конфигуратор больше не обещает `ULTRA` для 4K при низком бюджете без предупреждения.
- Добавлено предупреждение для 4K при бюджете ниже 160 000 ₽.
- Добавлена кнопка “Скопировать конфигурацию”.
- Добавлено пояснение, что расчёт ориентировочный.
- Конфигуратор показывает 3 ближайших реальных товара из VK под выбранный бюджет.
- Добавлены `role="radiogroup"` и `aria-checked` для групп выбора.
- Уменьшена яркость рамки и шума в конфигураторе.
- Добавлены `.gitignore` и `.env.example`.

## Выполнено следующим фронтовым проходом

- Каталог разделён на управляемые блоки: поиск, сортировка, фильтр бюджета и фильтр класса сборки.
- Добавлен поиск по названию, цене, CPU, GPU, RAM, SSD и характеристикам.
- Добавлена сортировка каталога: дешевле, дороже, мощнее.
- Добавлены фильтры класса: `Full HD`, `2K`, `Топ`.
- Карточки товаров получили chips: класс сборки, сценарий, CPU, GPU, RAM, SSD.
- CTA карточки усилен до “Написать по сборке”, вторичная кнопка ведёт в VK.
- Hero связан с выбранной рекомендуемой сборкой: показывает активный товар, цену и ссылку на него.
- Активная карточка hero центрируется при выборе.
- Конфигуратор получил ручной ввод бюджета.
- Конфигуратор получил выбор стиля корпуса: `Минимализм`, `RGB`, `Airflow`.
- Стиль корпуса добавлен в текст копируемой конфигурации.
- Фейковый телефон заменён на основной канал `vk.me/tog_pc`.
- Финальный CTA и footer переведены на VK-сообщения и конфигуратор.

## Выполнено следующим проходом: trust и каталог

- Блок “Почему TOGOSHOL” усилен условиями покупки.
- Добавлены trust-пункты: гарантия, сроки, оплата, выдача.
- Условия встроены без создания лишней секции, чтобы лендинг не раздувался.
- В каталоге добавлена кнопка “Показать ещё”.
- Каталог сначала показывает 9 товаров, остальные раскрываются по кнопке.
- При изменении поиска, фильтра или сортировки лимит каталога сбрасывается.
- Это уменьшает визуальную перегрузку и особенно помогает мобильной версии.

## Короткий вывод

Сайт уже имеет правильное направление: тёмный premium-tech стиль, сильный hero, реальные товары из VK, понятную структуру лендинга.

Но сейчас он выглядит как рабочий прототип, а не как готовый продающий сайт. Главные проблемы:

- первый экран перегружен и одновременно недособран;
- товарная лента выглядит как технический импорт, а не как премиальная витрина;
- многие секции слишком пустые;
- scroll-snap мешает обычному чтению и создаёт ощущение “залипания”;
- фон стал лучше, но всё ещё не управляет композицией страницы;
- реальные товары из VK перенесены, но данные нужно нормализовать;
- CTA и контакты всё ещё заглушки;
- мобильная версия почти наверняка будет ломаться на длинных названиях товаров;
- нет нормального сценария выбора ПК;
- нет доверия: нет адреса, условий, гарантии, сроков, реальных контактов, соцдоказательств.

---

# P0 — критично исправить в первую очередь

## 1. В шапке стоят заглушки контактов

Сейчас:

- телефон: `+7 000 000-00-00`;
- Telegram: `https://t.me/`;
- email частично заглушечный;
- кнопка “Написать” ведёт не к реальному контакту.

Почему плохо:

- пользователь не сможет купить;
- доверие падает сразу;
- сайт выглядит недоделанным.

Исправление:

- заменить телефон на реальный;
- заменить Telegram на реальный `vk.me/tog_pc` или Telegram, если он есть;
- кнопку “Написать” вести в основной канал продаж;
- в footer продублировать реальные контакты.

## 2. Товары из VK импортированы, но данные грязные

Сейчас в карточки попадает текст вида:

- “💬 Напиши прямо сейчас получи подарок к ПК!”;
- “Конфигурация:”;
- обрывки описаний;
- строки с рекламными фразами вместо характеристик;
- местами опечатки из VK, например “Игрвоой”.

Почему плохо:

- карточки выглядят неаккуратно;
- характеристики плохо сканируются;
- премиальность ломается;
- пользователь не видит быстро CPU / GPU / RAM / SSD / цену.

Исправление:

- добавить слой нормализации данных после импорта;
- отделить маркетинговое описание от характеристик;
- карточка должна показывать только:
  - CPU;
  - GPU;
  - RAM;
  - SSD;
  - PSU / cooling, если есть;
  - цену;
  - статус.
- рекламные фразы вынести в маленький badge или убрать.

## 3. Товарная лента слишком низко и выглядит обрезанной

Сейчас:

- карточки начинаются почти под fold;
- на первом скрине видны не полностью;
- горизонтальная прокрутка ощущается как случайная полоска;
- часть карточек уходит вправо без явного сигнала, что это карусель.

Почему плохо:

- пользователь может не понять, что товаров много;
- главный коммерческий блок выглядит второстепенно;
- карточки не продают с первого взгляда.

Исправление:

- сделать ленту отдельной частью hero с заголовком “В наличии и под заказ”;
- добавить короткий счётчик: “26 сборок из VK”;
- добавить стрелки влево/вправо;
- добавить snap внутри горизонтальной ленты;
- активную карточку центрировать при клике;
- на desktop показывать 3–4 карточки полностью, а не обрезанные края.

## 4. Карточки товаров визуально слабые

Сейчас:

- карточки тёмные, но похожи на одинаковые прямоугольники;
- фото слишком маленькое и уходит вниз;
- у неактивных карточек мало информации;
- у активной карточки нет полноценного раскрытого состояния;
- характеристики появляются, но занимают мало места;
- CTA виден только в активной карточке.

Почему плохо:

- товар не ощущается “дорогим”;
- фото из VK не раскрывает ценность;
- пользователь вынужден кликать вслепую.

Исправление:

- сделать карточку в 2 слоя:
  - сверху фото ПК;
  - снизу название, цена, ключевые specs.
- для активной карточки:
  - фото крупнее;
  - specs в две колонки;
  - отдельные chips: `CPU`, `GPU`, `RAM`, `SSD`;
  - CTA “Открыть товар VK”;
  - CTA “Написать по этой сборке”.
- для неактивной карточки показывать минимум:
  - фото;
  - GPU/CPU;
  - цену.

## 5. Нет полноценной секции каталога

Сейчас все товары спрятаны в горизонтальной ленте на hero.

Почему плохо:

- 26 товаров плохо просматривать только горизонтальным скроллом;
- нельзя сравнить;
- нет сортировки;
- нет быстрого поиска по бюджету.

Исправление:

- после hero добавить отдельную секцию “Все сборки”;
- сетка desktop: 3–4 карточки в ряд;
- tablet: 2 карточки;
- mobile: 1 карточка;
- добавить фильтры:
  - `до 60 000`;
  - `60 000–90 000`;
  - `90 000–150 000`;
  - `150 000+`;
  - `RTX 4060 / 4070 / 5080`;
  - `Intel / Ryzen`.

## 6. Scroll-snap сейчас мешает

Сейчас сайт прокручивается с привязкой к секциям.

Почему плохо:

- секции пустые, snap подчёркивает пустоту;
- пользователь теряет контроль;
- на тачпаде может быть неприятно;
- если контент секции больше экрана, часть может быть неудобно читать.

Исправление:

- отключить `scroll-snap-type` для обычного лендинга;
- если нужен слайдовый эффект, оставить только для desktop и только после визуального уплотнения секций;
- на mobile полностью отключить;
- заменить на плавные anchor transitions и reveal.

## 7. Второй, третий и четвёртый экраны слишком пустые

Сейчас:

- Custom Build занимает маленькую карточку в огромном пустом поле;
- Why TOGOSHOL выглядит как четыре карточки в пустоте;
- Order Process тоже висит в пустоте;
- Final CTA пустоват.

Почему плохо:

- сайт кажется недособранным;
- дорогое позиционирование не поддержано визуально;
- пользователь скроллит воздух.

Исправление:

- уменьшить высоту секций;
- убрать обязательный `min-height: 100svh` для всех `.section`;
- сделать секции естественной высоты;
- объединить некоторые блоки:
  - “Почему TOGOSHOL” + “Как проходит заказ” можно сделать одним экраном;
  - Custom Build можно расширить реальной формой подбора.

## 8. Общий фон всё ещё недостаточно управляемый

Сейчас:

- фон единый, но местами создаёт большие пустые тёмные поля;
- градиент не поддерживает композицию каждого блока;
- карточки выглядят как плавающие объекты без сцены.

Почему плохо:

- сайт выглядит пустым;
- визуальная глубина есть, но нет направления внимания;
- первый экран лучше остальных.

Исправление:

- сделать общий фон:
  - `body::before` — базовый gradient;
  - `body::after` — мягкая сетка/световая маска с opacity 0.05–0.08;
  - локальные glow только внутри секций.
- не использовать резкие секционные background-переходы;
- добавить faint radial glow за товарной лентой.

## 9. Hero badge обрезается шапкой

На скрине видно, что badge “Готовые и кастомные игровые ПК” частично залез под sticky header.

Почему плохо:

- первый экран выглядит сломанным;
- доверие падает за 1 секунду.

Исправление:

- увеличить верхний padding hero;
- учесть высоту sticky header;
- добавить `scroll-padding-top`;
- проверить при 1440, 1280, 1024.

## 10. Нет реального сообщения “что продаём”

H1 нормальный, но после импорта VK нужно точнее:

Сейчас:

> Игровые ПК в Великом Новгороде

Хорошо, но недостаточно.

Исправление:

- добавить под H1 строку:
  - “26 готовых сборок из наличия и конфигурации под заказ.”
- или:
  - “Готовые сборки от 47 900 ₽ и кастомные ПК под твой бюджет.”

## 11. Нет минимальной формы заявки

Сейчас пользователь может только нажать CTA.

Почему плохо:

- часть людей не любит писать в мессенджер без структуры;
- нет сбора намерения;
- нет выбора бюджета.

Исправление:

- добавить компактную форму:
  - имя;
  - телефон/Telegram;
  - бюджет;
  - задачи;
  - кнопка “Получить подбор”.
- без backend можно делать `mailto:` или VK/Telegram deep link с предзаполненным текстом.

## 12. Товары открываются в VK, но сайт не помогает выбрать

Сейчас карточка ведёт в VK, но сайт не делает работу консультанта.

Исправление:

- добавить бейджи:
  - “Full HD”;
  - “2K”;
  - “Стриминг”;
  - “Монтаж”;
  - “Топ за бюджет”.
- автоматически выводить их по GPU/CPU:
  - RTX 3050/3060/4060 — Full HD;
  - RTX 3070/3080/4070/5070 — 2K;
  - RTX 5080 — 4K/Ultra.

---

# P1 — важно для качества и продаж

## 13. Нужно нормализовать названия товаров

Примеры:

- `R5 8400 + RTX 5060`;
- `13400f/RTX5060/16GB`;
- `Игровой компьютер I5 13400 RTX3080 32GB DDR5`.

Проблема:

- нет единого формата;
- карточки скачут по высоте;
- часть названий кричит, часть выглядит технически.

Исправление:

- форматировать названия:
  - `Ryzen 5 8400F / RTX 5060 / 16GB DDR5`;
  - `Intel i5-13400F / RTX 4060 / 16GB`;
- сырой title из VK оставить как fallback;
- нормализованный title хранить отдельно.

## 14. Нужно извлекать цену как число

Сейчас цена строкой: `62 900 ₽`.

Исправление:

- добавить `priceValue`;
- сортировать товары по цене;
- показывать “от дешёвых к дорогим”;
- добавить фильтр бюджета.

## 15. Нужно обработать дубли товаров

В импорте есть похожие позиции:

- `Ryzen 5 5500 + RTX 3070 8GB`;
- `Игровой ПК` с RTX 3070;
- похожие Intel/Ryzen сборки.

Исправление:

- показывать все, но группировать по цене/GPU;
- или подсвечивать “похожие”;
- не допускать двух одинаковых названий.

## 16. Фото товаров из VK выглядят как рекламные карточки, а не предметные фото

Сейчас много изображений — постеры/баннеры с текстом.

Почему плохо:

- плохо сочетается с premium landing;
- мелкий текст на фото не читается;
- выглядит как маркетплейс, а не high-tech бренд.

Исправление:

- для главной ленты использовать общий premium cutout PC;
- VK-фото показывать в отдельном каталоге;
- или сделать image frame с dark overlay и blur backdrop;
- лучшие реальные фото ПК вынести вручную в assets.

## 17. Hero PC слишком “оторван” от товарной ленты

Сейчас большой ПК справа, а снизу карточки с другими VK-постерами.

Исправление:

- связать активный товар с hero visual:
  - при клике карточки менять hero title/price/photo;
  - справа показывать фото активного товара или fallback cutout;
  - hero CTA вести на активный товар.

## 18. Нет состояния “активная карточка выбрана”

Есть расширение, но оно не объясняет, что карточка выбрана.

Исправление:

- добавить label “Выбрано”;
- добавить cyan outline;
- добавить кнопку “Смотреть товар” всегда в активной карточке;
- при выборе обновлять hero summary.

## 19. Горизонтальная лента без стрелок

Исправление:

- добавить кнопки-стрелки слева/справа;
- на desktop — видимые;
- на mobile — swipe + dots;
- добавить `aria-label`.

## 20. Scrollbar выглядит технически

Сейчас нижняя полоска выглядит как дефолтный горизонтальный скролл.

Исправление:

- скрыть scrollbar;
- добавить кастомный progress indicator;
- или стилизовать как тонкую premium-линейку.

## 21. Нет мини-статистики по каталогу

Исправление:

- “26 товаров из VK”;
- “от 47 900 ₽”;
- “до 250 000 ₽”;
- “обновлено из VK”.

## 22. Нет даты обновления товаров

Исправление:

- при импорте писать timestamp;
- выводить “Обновлено: 04.06.2026”;
- хранить в `src/data/vkProductsMeta.ts`.

## 23. Нет fallback при битой картинке VK

Если URL фото истечёт/сломается, карточка будет пустой.

Исправление:

- добавить `onError` для картинки;
- fallback на `hero-pc.png`;
- для CSS — placeholder с градиентом.

## 24. Нет lazy loading для товарных фото

Сейчас 26 удалённых фото могут грузиться сразу.

Исправление:

- добавить `loading="lazy"`;
- добавить `decoding="async"`;
- возможно скачать фото локально при импорте.

## 25. Важный ассет `hero-pc.png` весит больше 1.3 MB

Почему плохо:

- медленнее первый экран;
- особенно плохо на мобильном интернете.

Исправление:

- конвертировать в WebP/AVIF;
- сделать 2 размера:
  - desktop;
  - mobile.
- использовать `<picture>`.

## 26. Нет SEO под реальные товары

Сейчас title/description общие.

Исправление:

- добавить meta description с ценами:
  - “Игровые ПК TOGOSHOL в Великом Новгороде: готовые сборки от 47 900 ₽, RTX/Ryzen/Intel, сборка под заказ.”
- добавить Open Graph;
- добавить JSON-LD `Product`/`ItemList` хотя бы статически.

## 27. Нет нормального footer trust-блока

Исправление:

- адрес/город;
- телефон;
- VK;
- Telegram;
- режим работы;
- гарантия;
- условия передачи/самовывоза.

## 28. Нет блока гарантий

Почему плохо:

- для ПК это сильное возражение;
- пользователь хочет знать, что будет после покупки.

Исправление:

- добавить “Гарантия и проверка”:
  - стресс-тест;
  - проверка температур;
  - гарантия на комплектующие;
  - помощь после покупки.

## 29. Нет условий оплаты

Исправление:

- добавить микро-блок:
  - оплата наличными/переводом;
  - предоплата для заказа;
  - итоговая смета перед сборкой.

## 30. Нет сроков сборки

Исправление:

- “Готовые ПК — по наличию”;
- “Под заказ — обычно X дней”;
- если точных сроков нет, написать “срок зависит от комплектующих”.

## 31. Нет сценария для родителей

Целевая аудитория включает родителей, но текст полностью для геймеров.

Исправление:

- добавить фразу:
  - “Объясним простым языком, какая сборка подойдёт для игр, учёбы и бюджета.”
- добавить карточку “Для ребёнка / учёбы / игр”.

## 32. Нет сценария для стримеров/монтажа

Исправление:

- добавить тег “стриминг/монтаж” к мощным сборкам;
- в Custom Build указать OBS, монтаж, 3D, AI.

## 33. Нет сравнения “готовый ПК vs под заказ”

Исправление:

- сделать два варианта:
  - “Выбрать готовый”;
  - “Собрать под задачу”.
- в hero CTA разделить эти сценарии.

## 34. Нет быстрого “что выбрать по бюджету”

Исправление:

- добавить бюджетные chips:
  - до 60k;
  - 60–80k;
  - 80–120k;
  - 120k+.

## 35. Название TOGOSHOL не раскрыто

Бренд есть, но нет чувства бренда.

Исправление:

- добавить микро-строку:
  - “TOGOSHOL PC — локальные игровые сборки в Великом Новгороде.”
- использовать единый логотип/mark.

---

# P2 — polish и техническое качество

## 36. Цветовая палитра уходит в однообразный сине-фиолетовый фон

Исправление:

- добавить больше нейтрального чёрного/графитового;
- cyan использовать как акцент;
- зелёный только для “В наличии”;
- violet только для depth glow.

## 37. Карточки слишком большие по радиусу для товарной сетки

Сейчас radius 28px, выглядит чуть игрушечно.

Исправление:

- для товарных карточек 18–22px;
- для hero/final CTA можно оставить 28–32px.

## 38. Текст в карточках может не помещаться

Длинные названия:

- `Игровой компьютер I5 13400 RTX3080 32GB DDR5`;
- `Ryzen 5 5500 + RX6700XT 12GB`.

Исправление:

- `line-clamp`;
- max 2–3 строки;
- title меньше в неактивных карточках;
- active card показывает полный title.

## 39. Нужны hover/focus states

Исправление:

- `:focus-visible` для карточек;
- `:focus-visible` для кнопок;
- visible outline cyan.

## 40. Сейчас карточка `article role=button` спорная по accessibility

Исправление:

- сделать настоящую `<button>` для раскрытия;
- ссылку вынести отдельной кнопкой;
- или разделить card header button и CTA link.

## 41. Не хватает keyboard navigation для ленты

Исправление:

- стрелки клавиатуры переключают карточки;
- Home/End переходят к первой/последней;
- active card scrollIntoView.

## 42. Нет loading/error states

Сейчас данные статические, но импорт может ломаться.

Исправление:

- добавить комментарии в `vkProducts.ts`;
- добавить fallback `builds`;
- если массив пустой, показывать “Товары обновляются”.

## 43. Импорт VK не скачивает фото локально

Почему плохо:

- VK URL могут меняться;
- картинки зависят от внешнего CDN;
- SEO/скорость хуже.

Исправление:

- расширить `scripts/import-vk-market.mjs`;
- скачивать фото в `src/assets/products`;
- генерировать локальные пути.

## 44. Импорт VK не чистит описание

Исправление:

- добавить функции:
  - `extractCpu`;
  - `extractGpu`;
  - `extractRam`;
  - `extractStorage`;
  - `extractPsu`;
  - `normalizeTitle`;
  - `normalizePrice`.

## 45. Нет теста на импорт

Исправление:

- добавить `scripts/validate-products.mjs`;
- проверять:
  - title;
  - price;
  - vkUrl;
  - specs length;
  - image.

## 46. Нет ESLint/Prettier

Исправление:

- добавить ESLint;
- добавить Prettier;
- добавить `npm run lint`;
- добавить форматирование.

## 47. Нет README по обновлению товаров

Исправление:

- создать `PRODUCT_IMPORT.md`;
- описать:
  - где взять токен;
  - как запустить импорт;
  - как проверить;
  - как отозвать токен.

## 48. В repo попали dev logs

Сейчас есть:

- `vite-dev.log`;
- `vite-dev.err.log`;
- `dist`;
- `node_modules`.

Если это станет git repo, это нельзя коммитить.

Исправление:

- добавить `.gitignore`;
- исключить:
  - `node_modules`;
  - `dist`;
  - `*.log`;
  - `.env`.

## 49. Нет env-файла для токена

Исправление:

- создать `.env.example`;
- указать:
  - `VK_TOKEN=`;
- токен не хранить в repo.

## 50. Токен уже был вставлен в чат

Почему важно:

- его нужно считать скомпрометированным.

Исправление:

- после импорта отозвать/перевыпустить токен;
- не хранить токен в файлах;
- не писать его в docs.

## 51. Нет Playwright-визуальной проверки

Исправление:

- добавить Playwright;
- сделать screenshots:
  - 1440;
  - 1280;
  - 768;
  - 390;
  - 360.

## 52. Нет проверки горизонтального overflow

Исправление:

- в Playwright проверять:
  - `document.documentElement.scrollWidth <= window.innerWidth`;
- особенно mobile.

## 53. Нет аналитики кликов

Исправление:

- хотя бы data attributes:
  - `data-product-title`;
  - `data-product-price`;
  - `data-product-vk-url`.
- позже подключить Метрику/VK Pixel.

## 54. Нет UTM/track параметров в VK ссылках

Исправление:

- добавить к ссылкам `?utm_source=site&utm_medium=landing`;
- если VK product URL плохо принимает query, использовать ссылку на сообщения с предзаполненным текстом.

## 55. CTA “Открыть в VK” слабее, чем “Написать по этой сборке”

Исправление:

- primary: “Написать по этой сборке”;
- secondary: “Открыть товар VK”.

## 56. Нет предзаполненного сообщения

Исправление:

- формировать ссылку:
  - `https://vk.me/tog_pc?ref=...`
- или копировать текст:
  - “Здравствуйте, интересует сборка Ryzen 5 5500 + RTX 3070 за 62 900 ₽”.

## 57. Не хватает доверительного текста рядом с ценой

Исправление:

- “Цена из VK”;
- “Актуальность уточняйте перед заказом”;
- “Можно изменить корпус/память/накопитель”.

## 58. “В наличии” для всех товаров может быть неверным

Импорт сейчас ставит `В наличии`, если VK availability 0.

Проверить:

- как VK возвращает availability;
- нет ли товаров “под заказ”, которые формально доступны.

Исправление:

- явно маппить статусы;
- для товара “Под заказ” показывать `Под заказ`, даже если VK status говорит иначе.

## 59. “Под заказ” как название товара выглядит странно

Первый товар называется `Под заказ`.

Исправление:

- сделать его не товаром в ленте, а отдельным custom-build card;
- или переименовать display title:
  - “Сборка под заказ”.

## 60. На главной слишком много веса на один hero

Сейчас hero содержит:

- H1;
- текст;
- CTA;
- метрики;
- фото;
- 26 товаров.

Исправление:

- hero оставить как selling screen;
- товары вынести ниже в полноценный каталог;
- в hero оставить 4–6 рекомендуемых товаров.

---

# Предлагаемый новый порядок блоков

1. Header.
2. Hero:
   - H1;
   - цена “от 47 900 ₽”;
   - CTA;
   - hero PC;
   - 3 коротких преимущества.
3. Featured Builds:
   - 6 лучших товаров из VK;
   - фильтр по бюджету.
4. Full Catalog:
   - все 26 товаров;
   - сетка;
   - фильтры.
5. Custom Build:
   - подбор под бюджет;
   - mini form.
6. Warranty / Trust:
   - гарантия;
   - стресс-тест;
   - локально;
   - помощь после покупки.
7. Order Process.
8. Final CTA.
9. Footer.

---

# Конкретный план исправлений

## Шаг 1. Срочный visual fix

- убрать `min-height: 100svh` у всех секций;
- отключить `scroll-snap`;
- поднять hero badge ниже header;
- сделать hero компактнее;
- сделать товарную ленту не обрезанной;
- добавить стрелки ленты;
- скрыть системный scrollbar.

## Шаг 2. Нормализация товаров

- добавить парсер характеристик из VK description;
- создать поля:
  - `cpu`;
  - `gpu`;
  - `ram`;
  - `storage`;
  - `psu`;
  - `priceValue`;
  - `useCase`;
  - `normalizedTitle`.
- убрать мусорные строки из specs.

## Шаг 3. Новая карточка товара

- фото сверху;
- цена крупно;
- CPU/GPU/RAM/SSD chips;
- CTA “Написать по сборке”;
- secondary CTA “Открыть VK”;
- active-card expansion оставить, но сделать более полезным.

## Шаг 4. Полноценный каталог

- отдельная секция с grid;
- filters;
- sorting by price;
- “Показать ещё” на mobile.

## Шаг 5. Доверие и продажа

- реальные контакты;
- условия гарантии;
- сроки;
- оплата;
- адрес/город;
- процесс заказа;
- предзаполненные сообщения.

## Шаг 6. Технический порядок

- `.gitignore`;
- `.env.example`;
- README по импорту VK;
- image optimization;
- Playwright screenshots.

---

# Быстрые исправления, которые я бы сделал сразу

1. Отключить scroll-snap.
2. Снять `min-height: 100svh` с секций.
3. Сделать отдельный каталог всех VK-товаров.
4. Оставить в hero только 6 рекомендуемых товаров.
5. Добавить стрелки карусели.
6. Нормализовать характеристики.
7. Заменить “Открыть в VK” на “Написать по этой сборке”.
8. Подставить реальные телефон/VK/Telegram.
9. Сжать hero image в WebP.
10. Добавить `.gitignore`.
11. Доработать конфигуратор ПК: сделать расчёт ближе к реальным товарам из VK.
12. Добавить в конфигуратор выбор “новый / б/у / не важно”.
13. Добавить в конфигуратор выбор корпуса, подсветки и объёма SSD.
14. Исправить ссылку заявки из конфигуратора: VK может не принять параметр `message`, лучше вести в `vk.me/tog_pc` и давать копируемый текст.
15. Связать результат конфигуратора с товарами VK: показывать 3 ближайшие реальные сборки под выбранный бюджет.

---

# Аудит модуля конфигуратора ПК

Файл модуля: `src/components/CustomBuild/CustomBuild.tsx`  
Стили: `src/components/CustomBuild/CustomBuild.css`  
Состояние: модуль рабочий, интерактивный, но пока больше похож на красивый калькулятор-демо, чем на точный коммерческий подбор.

## Что в модуле уже хорошо

- Есть понятная идея: пользователь задаёт бюджет, игру и разрешение.
- Слайдер бюджета работает.
- Сегментированные кнопки игры и разрешения работают.
- Правая карточка пересчитывается без перезагрузки.
- Есть рекомендованный класс сборки: START / PRO / ULTRA.
- Есть комплектующие: GPU, CPU, RAM, SSD, охлаждение.
- Есть оценка FPS.
- Есть CTA на обсуждение сборки.
- Визуально блок ближе к premium-tech, чем старый статичный custom-блок.
- На desktop структура понятная: слева управление, справа результат.

## Главная проблема

Конфигуратор сейчас считает условную рекомендацию сам по себе, а не опирается на реальные товары TOGOSHOL из VK.

Из-за этого может получиться конфликт:

- в каталоге есть реальные сборки за конкретную цену;
- конфигуратор рекомендует условный RTX 4070 Super / RTX 5080;
- пользователь нажимает CTA, а такой конкретной сборки может не быть в наличии.

Исправление:

- использовать `vkProducts` как источник реальных вариантов;
- подбирать ближайшие товары по бюджету;
- показывать “лучшие совпадения из наличия”;
- если точного совпадения нет, писать “под заказ”.

## Проблемы логики расчёта

### 1. FPS считается слишком грубо

Сейчас формула:

- берёт базовый FPS по игре;
- умножает на бюджет;
- умножает на коэффициент разрешения.

Почему плохо:

- бюджет не равен производительности;
- RTX 3060 Ti и RTX 5060 могут вести себя по-разному;
- CPU важен для CS2;
- 4K зависит почти полностью от GPU;
- “Работа / 3D / AI” вообще не должна показывать FPS как главный показатель.

Исправление:

- сделать таблицу GPU → performance score;
- сделать таблицу game/resolution → fps multiplier;
- для `Работа / 3D / AI` вместо FPS показывать:
  - “уровень рендера”;
  - “память для проектов”;
  - “AI/3D запас”.

### 2. Выбор 4K всегда включает ULTRA

Сейчас любое 4K автоматически даёт `Флагманский ULTRA`.

Почему плохо:

- пользователь с бюджетом 70 000 ₽ может выбрать 4K и увидеть RTX 5080;
- это создаёт нереалистичное ожидание.

Исправление:

- если бюджет низкий и выбран 4K, показывать warning:
  - “Для 4K комфортный бюджет обычно выше 160 000 ₽.”
- класс должен учитывать и бюджет, и разрешение, но не игнорировать ограничение бюджета.

### 3. Конфигуратор не показывает компромиссы

Сейчас он всегда уверенно рекомендует сборку.

Исправление:

- добавить блок “Компромисс”:
  - “В этом бюджете лучше 1080p/1440p, чем 4K”;
  - “Можно взять мощнее GPU, но SSD оставить 512GB”;
  - “Для монтажа лучше поднять RAM до 32GB”.

### 4. Нет выбора “новые / б/у комплектующие”

Для локального рынка ПК это критично.

Исправление:

- добавить segmented control:
  - `Новые`;
  - `Б/у выгоднее`;
  - `Не важно`.
- это влияет на рекомендации и цену.

### 5. Нет выбора эстетики

У TOGOSHOL визуальный фокус на RGB/корпусах, но конфигуратор не спрашивает эстетику.

Исправление:

- добавить выбор:
  - `Минимализм`;
  - `RGB`;
  - `Белый корпус`;
  - `Максимальный airflow`.

### 6. Нет выбора SSD

Исправление:

- добавить `512GB / 1TB / 2TB`;
- для игр 1TB лучше по умолчанию;
- для бюджетных сборок 512GB.

### 7. Нет выбора RAM

Исправление:

- добавить `16GB / 32GB / 64GB`;
- для `Работа / 3D / AI` 32GB минимум.

### 8. Нет выбора “монитор уже есть?”

Разрешение монитора есть, но нет вопроса про частоту.

Исправление:

- добавить частоту:
  - `60–75Hz`;
  - `144–180Hz`;
  - `240Hz+`.
- для CS2 это важнее, чем для Cyberpunk.

## Проблемы UX

### 9. Слайдер бюджета может быть неточным

Сейчас step `5000`, но нет ручного ввода.

Исправление:

- оставить slider;
- добавить numeric input рядом;
- пользователь может ввести `85000`.

### 10. Активные кнопки яркие, но все режимы одинаковые по весу

Исправление:

- игру и разрешение визуально разделить;
- у игры можно добавить маленькие подписи:
  - “тяжёлая графика”;
  - “киберспорт”;
  - “рабочая нагрузка”.

### 11. CTA расположен только снизу слева

На длинном блоке пользователь может смотреть на правую карточку и не заметить кнопку.

Исправление:

- добавить CTA и в правую карточку:
  - “Отправить эту конфигурацию”.

### 12. Нет состояния “копировать конфигурацию”

Исправление:

- добавить кнопку:
  - “Скопировать конфигурацию”;
- копировать текст в clipboard;
- показывать toast “Скопировано”.

### 13. VK link с `message` может не сработать как ожидается

Сейчас ссылка:

`https://vk.me/tog_pc?message=...`

Проблема:

- VK может игнорировать параметр;
- в некоторых клиентах он не подставится;
- пользователь откроет пустой диалог.

Исправление:

- оставить ссылку на `https://vk.me/tog_pc`;
- рядом добавить копируемый текст;
- или использовать виджет/форму, если будет backend.

### 14. Нет явного предупреждения, что расчёт ориентировочный

Исправление:

- добавить small text:
  - “Расчёт ориентировочный. Итоговую конфигурацию уточним по наличию и ценам комплектующих.”

### 15. Нет связи с товарными карточками

Исправление:

- после расчёта показать:
  - “Ближайшие готовые сборки”;
  - 3 карточки из `vkProducts`, ближайшие по цене;
  - CTA на каждую.

## Проблемы визуала

### 16. Блок выглядит лучше старого, но слишком похож на макет из референса

Исправление:

- добавить TOGOSHOL-уникальность:
  - real PC image на фоне правой карточки;
  - small accent “TOGOSHOL Configurator”;
  - использовать реальные товары/цены.

### 17. Noise overlay внутри карточки может выглядеть грязно

Сейчас `configurator::before` добавляет шум.

Проблема:

- на тёмном фоне может выглядеть как jpeg/грязь;
- ранее уже была претензия к фону.

Исправление:

- уменьшить opacity до 0.08–0.12;
- или убрать noise полностью;
- оставить чистые radial gradients.

### 18. Большая белая рамка может выглядеть слишком резко

Сейчас border `rgba(255,255,255,0.38)`.

Исправление:

- сделать `rgba(255,255,255,0.14)`;
- на hover/active можно усиливать;
- добавить мягкий внутренний glow вместо яркой рамки.

### 19. Cyan headline хороший, но может конкурировать с CTA

Исправление:

- оставить cyan только в одном слове;
- CTA сделать главным cyan/white элементом;
- статус “Оптимально” оставить зелёным.

### 20. Правая карточка статична

Исправление:

- при изменении бюджета мягко анимировать specs;
- добавить маленький progress/score indicator;
- при ULTRA — более сильный glow.

## Адаптивность

### 21. На mobile блок может стать слишком длинным

Проблема:

- слайдер;
- 3 кнопки игр;
- 3 кнопки разрешения;
- included list;
- recommendation card;
- CTA.

Исправление:

- на mobile порядок:
  1. заголовок;
  2. controls;
  3. compact recommendation;
  4. CTA;
  5. included list свернуть.

### 22. Длинная кнопка CTA может не помещаться

Текст:

`Обсудить эту сборку с инженером`

Исправление:

- mobile text:
  - “Обсудить сборку”;
- desktop оставить полный.

### 23. Segmented buttons с длинным текстом могут ломаться

Особенно:

`Работа / 3D / AI`

Исправление:

- на mobile сделать вертикально;
- на desktop оставить 3 колонки;
- добавить `min-height` и `text-wrap: balance`.

## Accessibility

### 24. Группы кнопок не оформлены как radiogroup

Сейчас это обычные button.

Исправление:

- добавить `role="radiogroup"`;
- кнопкам `role="radio"`;
- `aria-checked`;
- или оставить button, но добавить понятные `aria-label`.

### 25. Range input без видимой шкалы значений по центру

Исправление:

- добавить текущее значение крупно;
- добавить `aria-valuetext`.

### 26. `aria-live` на всей правой карточке может быть шумным

При каждом движении слайдера screen reader может читать слишком много.

Исправление:

- `aria-live="polite"` оставить только на короткой строке результата;
- или обновлять после debounce.

## Технические улучшения

### 27. Логику расчёта нужно вынести из компонента

Сейчас функции в `CustomBuild.tsx`.

Исправление:

- создать `src/data/configurator.ts` или `src/lib/configurator.ts`;
- вынести:
  - games;
  - resolutions;
  - getRecommendation;
  - formatPrice;
  - performance tables.

### 28. Нужны тесты на расчёт

Исправление:

- добавить unit cases:
  - 50k + 4K не должен давать RTX 5080 без предупреждения;
  - work mode должен давать 32GB RAM;
  - 300k + 4K должен давать ULTRA.

### 29. Нужна связь с `vkProducts`

Исправление:

- парсить `vkProducts`;
- находить ближайшие товары:
  - price <= budget;
  - matching GPU tier;
  - matching use case.

### 30. Нужен формат заявки как отдельная функция

Исправление:

- `buildConfiguratorMessage(config)` вынести отдельно;
- использовать и для VK, и для clipboard.

## Что бы я исправил в модуле первым

1. Убрал слишком яркую белую рамку.
2. Уменьшил или убрал noise overlay.
3. Добавил предупреждение “ориентировочный расчёт”.
4. Добавил правый CTA “Отправить эту конфигурацию”.
5. Добавил кнопку “Скопировать конфигурацию”.
6. Добавил выбор `новые / б/у / не важно`.
7. Добавил выбор SSD и RAM.
8. Сделал расчёт 4K честнее: при низком бюджете предупреждение, а не ULTRA.
9. Связал рекомендации с реальными товарами VK.
10. Вынес логику расчёта в отдельный файл.
11. Добавил 3 ближайшие сборки из VK под выбранный бюджет.
12. Проверил mobile 360/390/430.

## Предлагаемые изменения в коде

### Быстрый патч

- `CustomBuild.css`:
  - border `0.38` заменить на `0.14`;
  - noise opacity `0.32` заменить на `0.1`;
  - добавить `.configNote`;
  - добавить `.copyConfigButton`;
  - добавить mobile-short CTA.

### Средний патч

- `CustomBuild.tsx`:
  - добавить state `partCondition`;
  - добавить state `ramChoice`;
  - добавить state `storageChoice`;
  - добавить warning при 4K и бюджете ниже 160k;
  - добавить copy-to-clipboard;
  - добавить “Ближайшие товары из VK”.

### Большой патч

- создать `src/lib/configurator.ts`;
- создать `src/components/ProductMatch/ProductMatch.tsx`;
- подключить подбор ближайших товаров;
- добавить unit tests для расчёта.

---

# Что нужно от владельца проекта

Чтобы довести сайт до продающего состояния, нужны реальные данные:

- рабочий телефон;
- рабочий VK message link;
- Telegram, если используется;
- условия гарантии;
- сроки сборки;
- условия оплаты;
- адрес или формат выдачи;
- какие товары точно “в наличии”, а какие “под заказ”;
- какие 4–6 товаров считать рекомендуемыми;
- можно ли скачивать фото из VK локально.

---

# Итог

Главная проблема сайта сейчас не в том, что “всё плохо”. Проблема в том, что он застрял между двумя форматами:

- премиальный лендинг;
- сырой импорт VK-товаров.

Нужно разделить роли:

- hero продаёт бренд и быстро ведёт к подбору;
- каталог показывает все реальные товары;
- кастомный блок объясняет сборку под заказ;
- trust-блок снимает страхи;
- CTA ведёт в реальный канал продаж.

После этих правок сайт будет выглядеть не как прототип, а как нормальная локальная витрина игровых ПК.
